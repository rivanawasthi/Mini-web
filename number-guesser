<!Doctype html>
<html lang="en-US">
 <head>
    <meta charset="utf-8">

    
    <title>numbber guessing game</title>

<styles>
    html{
      <font-family: sans-serif;
    }

    body {
      width:50%:
      max-width:800px;
      min-width:480px;
      margin: 0 auto ;

    }

    .form input[type="number"] {
        width;200px;
    }

    .lastResult {
        color.white;
        paddings:3px;
    }
 </style>
</head>

  <body>
    <h1>Number guessing game</h1>

    <p>We have selected a random number between 1 and 100. See if you can guess it in 10 turns or fewer.We'll tell you if your guess was too high or too low<p>

    <div class="form">
        <label for="guessFieled">Enter a guess: </label>
        <input type"number" min="100" required id="guessField" class="guessField">
        <input type="submit" value="Submit guess" class="guess submit">
    </div>


</div class="form">
  <label for="guessField">Enter a guess: </label>
  <input type="number" min="100" required id="guessField" class="guessField">
  <input type="submit" value="Submit guess" class="guessSubmit">
</div


</div class="resultParas">
<p class="guesses"></p>
<p class="lastresult"></p>
<p class="low0rhi"></p>

</div


<script>

let randomNumber = Math.floor(Math.random() * 100) +1;


const guesses = document.querrySelector(".guess");
const lastResult = document.querrySelctor(".lastResult");
const low0rhi = document.querrySelector(".low0rhi");

const guessSubmit = document.querrySelector(".guessSubmit")
const guessField = document.querySelector(".guessField");

let guessCount = 1
let resetButton;

function checkGuess()
  const userguess=Number guessField.value
  if guessCount ===1
    guesses.textContent = "Previous guesser"
    
guesses.textContent = guesses textContent userGuess

if (userGuess === randomNumber) {
    lastResult textcontent = "Congratulations! You go it right!";
    lastResult style backgroundColor = "green"; 
    low0rhi.textContent = ""
    setGameOver();
}

]else[
  lastResult.textContent ="Wrong"
  lastResul.style.backgroundColor = "red"
  if (userGuess < randomNumber){
    low0rhi.textContent = "Last guess was to high";
  }



guessCount++;
guessField.value = "";
guessField.focus();



let number1 = 1;
number1 += 2;

</script>
</body>
</html>